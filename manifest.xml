<manifest version="2.0" type="device" target-level="6">

    <!-- Keymaster HAL -->
    <hal format="hidl">
        <n>android.hardware.keymaster</n>
        <transport>hwbinder</transport>
        <fqname>@4.0::IKeymasterDevice/default</fqname>
    </hal>

    <!-- Fingerprint HAL — jasmine uses Goodix -->
    <hal format="hidl">
        <n>vendor.goodix.hardware.fingerprint</n>
        <transport>hwbinder</transport>
        <fqname>@1.0::IGoodixBiometricsFingerprint/default</fqname>
    </hal>

</manifest>        <fqname>@2.0::IFingerprintSensorTest/default</fqname>
    </hal>
</manifest>
